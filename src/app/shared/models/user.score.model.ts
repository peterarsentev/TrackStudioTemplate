export class UserScoreModel {
  constructor(
    public conductScore?: number,
    public countConduct?: number,
    public countPass?: number,
    public id?: number,
    public passScore?: number,
    public userId?: number
  ) {
  }
}
