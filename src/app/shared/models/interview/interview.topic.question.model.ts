export class InterviewTopicQuestionModels {
  constructor(
    public id?: number,
    public topicName?: string,
    public topicId?: number,
    public questionTitle?: string,
    public questionId?: string,
  ) {
  }
}
