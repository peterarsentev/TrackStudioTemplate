import { RecentlySolvedModels } from './recently-solved-models';

describe('RecentlySolvedModels', () => {
  it('should create an instance', () => {
    expect(new RecentlySolvedModels()).toBeTruthy();
  });
});
